// Shared page shell width. Topbar and main content use the same value so the
// search bar always lines up with the cards underneath it.
export const shellContainer = 'mx-auto w-full max-w-[1800px] px-4 sm:px-6 xl:px-8';
